# Copy to web/.env and edit if the API runs somewhere other than the default.
# web/.env is gitignored.

# Astro only exposes PUBLIC_-prefixed variables to client code; the same
# value is also used for build-time (Node-context) fetches.
PUBLIC_API_BASE_URL=http://localhost:3001
